[package]
name = "unruster-facts"
version = "0.1.0"
edition = "2024"
description = "Pure data types and persistence helpers shared between the UnRuster driver and viewer."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs = "5"
